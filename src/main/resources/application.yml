spring:
  datasource:
    url: jdbc:mysql://localhost:3306/workouttracker
    username: root
    password: ilovebiking
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
